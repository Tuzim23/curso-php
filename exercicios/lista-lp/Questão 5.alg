algoritmo "semnome"
// 5) Escreva um algoritmo para ler um valor (do teclado) e escrever (na tela) o seu antecessor.
var
a , b : inteiro
inicio

a := 9
escreval("a = ",a)
b := a-1
escreval("b = ",b)

fimalgoritmo
